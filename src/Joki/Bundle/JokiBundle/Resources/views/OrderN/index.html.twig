{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OrderN list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Totalprice</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ordern_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.totalprice }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ordern_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ordern_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ordern_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
